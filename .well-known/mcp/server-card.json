{
  "serverInfo": {
    "name": "Solana Pulse XaaS",
    "version": "0.26.2"
  },
  "authentication": {
    "required": false,
    "schemes": []
  },
  "tools": [
    {
      "name": "sample_pulse",
      "description": "Free live Solana slot/epoch sample",
      "inputSchema": { "type": "object", "properties": {} }
    },
    {
      "name": "jupiter_quote",
      "description": "Paid Jupiter swap quote. 0.01 USDC.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "inputMint": { "type": "string" },
          "outputMint": { "type": "string" },
          "amount": { "type": "string" }
        },
        "required": ["inputMint", "outputMint", "amount"]
      }
    },
    {
      "name": "web_search",
      "description": "Paid web search. 0.01 USDC.",
      "inputSchema": {
        "type": "object",
        "properties": { "q": { "type": "string" } },
        "required": ["q"]
      }
    },
    {
      "name": "extract_url",
      "description": "Paid HTTPS fetch + text extract. 0.02 USDC.",
      "inputSchema": {
        "type": "object",
        "properties": { "url": { "type": "string" } },
        "required": ["url"]
      }
    },
    {
      "name": "license_scan",
      "description": "Paid license heuristic. 0.10 USDC.",
      "inputSchema": {
        "type": "object",
        "properties": { "packages": { "type": "string" } },
        "required": ["packages"]
      }
    },
    {
      "name": "install_preflight",
      "description": "Paid install-safety heuristic. 0.15 USDC.",
      "inputSchema": {
        "type": "object",
        "properties": { "packages": { "type": "string" } },
        "required": ["packages"]
      }
    }
  ]
}
